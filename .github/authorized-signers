# Authorized GPG signers for production release tags.
#
# [Audit 9 P0-1] Only keys listed here may sign production release tags (v*).
# CI verifies the tag signature against this list — unauthorized signers
# are BLOCKED from creating a GitHub Release.
#
# Format: one GPG key ID (or fingerprint suffix) per line.
# Lines starting with # are comments.
# Key IDs are matched case-insensitively, suffix-match (last 8-16 hex chars).
#
# To find your GPG key ID:
#   gpg --list-secret-keys --keyid-format=long
# Look for the line starting with "sec" or "rsa" — the key ID is after "/".
# Example: rsa4096/ABCD1234EFGH5678  → key ID = ABCD1234EFGH5678
#
# Add your key ID below (replace the placeholder):
5E0BB8EF44199645

# NOTE: This file is checked into the repo. For higher security, use the
# CI secret AUTHORIZED_SIGNERS instead (newline-separated key IDs) so the
# list is not visible to anyone with read access. The CI job checks both
# sources and merges them.
